import { Injectable, Inject } from '@nestjs/common';
import { AwsClient } from '../utils/awsClient';

@Injectable()
export class GoogleService {
    constructor() {}

    async createLoyaltyCard() {//(memberId: string, isUpdate: boolean, token?: string) {
        
    }
}